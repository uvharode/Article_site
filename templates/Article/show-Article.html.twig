{% extends 'base.html.twig' %} 
{# {% block title %}{{article.title}}{% endblock %} #}
{% block body %}
<div class="container">
{# <a href="{{ path('timeline') }}" class="btn btn-danger">TimeLine</a> #}
  {# <div class="row"> #}
    <div class="col-md-10">
      <div class="card bg-light mb-5 mt-3">
        <div class="card-body">
          {# <div class="row"> #}
              <div class="col-sm-15 blog-main">
                  <div class="blog-post">
                      <h2 class="blog-post-title">{{ article.title }}</h2>
                      <h5>
                      <i class="blog-post-author">
                              <p class="h5-danger"> by {{ article.author }}</p> 
                      </i>
                        </h5> 
                        <figure class = "card bg-light sm-10">
                      <img class = "img-thumbnail" 
                      src="{{ asset('uploads/content/' ~ article.image) }}" 
                      alt="{{ article.image }}">
                      </img>
                      </figure>
                      
                      <p>{{ article.content }} </p>
                            <i>
                              {# <h5>Username : {{ user.username }}</h5> #}
                              {# <h5>Email : {{ email }} </h5> #}
                            </i>
                            {# {% if user.username == username %} #}
                      <a href="/article/edit/{{username}}/{{ article.id }}" class="btn btn-warning">Edit<a/>
                            {# {% endif %} #}
                            
                      {# <div>
                             {{ form_start(form) }}
                              <div class="form_group">
                                <div class="col-md-12 mb-3">
                                  {{ form_row(form.commentData, {'attr': {'class': 'form-control'}}) }}
                                </div>
                              </div>
                               <div class="form-group">
            <div class="col-md-8 col-md-offset-4" style="margin-top:5px;">
              <button type="submit" class="btn btn-primary">
                <i class="fa fa-btn fa-user"></i> Submit
              </button>
            </div>
          </div>
                              {{ form_end(form) }}
                      </div> #}

                  </div>
                  
              {# </div> #}
          {# </div> #}
        </div>
      </div>  
    </div>
  </div>
</div>
{% endblock %}